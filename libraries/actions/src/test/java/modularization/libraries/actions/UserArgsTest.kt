package modularization.libraries.actions

import org.junit.Assert.assertTrue
import org.junit.Test

class UserArgsTest {

    @Test
    fun `test business logic of custom args classes`() {
        // if there is any business logic in the ***Args classes, it should be tested
        assertTrue(true)
    }
}